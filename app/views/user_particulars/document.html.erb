<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Upload UI</title>
    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #f5f5f5;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .guide {
            background-color: #ffebee;
            padding: 10px;
            border-radius: 10px;
            margin: 10px;
            width: 90%;
            max-width: 900px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            text-align: center;
        }

        .guide h2 {
            margin-top: 0;
            font-size: 20px;
        }

        .guide p, .guide ol {
            margin: 5px 0;
            font-size: 14px;
        }

        .upload-container {
            background: #fff;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            width: 90%;
            max-width: 900px;
            display: flex;
            justify-content: space-between;
            gap: 20px;
        }

        .upload-box {
            width: 45%;
            text-align: center;
            padding: 20px;
            border: 2px dashed #d9e3f0;
            border-radius: 10px;
            color: #90a4ae;
            cursor: pointer;
            box-sizing: border-box;
            position: relative;
        }

        .upload-box:hover {
            border-color: #42a5f5;
            color: #42a5f5;
        }

        .upload-box .icon {
            font-size: 40px;
        }

        .upload-box button {
            background-color: #42a5f5;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 20px;
        }

        .upload-box button:hover {
            background-color: #1e88e5;
        }

        .uploaded-files {
            width: 45%;
            max-height: 300px;
            overflow-y: auto;
        }

        .uploaded-files ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .uploaded-files li {
            display: flex;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #f0f0f0;
            word-wrap: break-word;
        }

        .uploaded-files li img {
            width: 24px;
            height: 24px;
            margin-right: 10px;
        }

        .uploaded-files li .file-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex: 1;
        }

        .uploaded-files li .file-name {
            display: flex;
            align-items: center;
            flex: 1;
            overflow: hidden;
        }

        .uploaded-files li .file-size,
        .uploaded-files li .upload-date,
        .uploaded-files li .status {
            font-size: 12px;
            color: #666;
        }

        .uploaded-files li .delete-btn {
            color: #e57373;
            cursor: pointer;
            margin-left: 10px;
        }

        .progress-bar {
            height: 5px;
            width: 100px;
            background-color: #f0f0f0;
            margin-left: 10px;
            border-radius: 2px;
            overflow: hidden;
        }

        .progress-bar div {
            height: 100%;
            background-color: #42a5f5;
            width: 0;
        }

        #fileInput {
            display: none;
        }
    </style>
</head>
<body>
    <div class="guide">
        <h2>File Upload Guide</h2>
        <p>Follow the instructions below to upload your documents:</p>
        <ol>
            <li>Drag and drop your files into the designated area or click the "Browse" button to select files from your computer.</li>
            <li>Supported file formats are PDF, DOC, DOCX, and JPG.</li>
            <li>The maximum file size allowed is 5MB.</li>
            <li>After selecting the files, they will be automatically uploaded and listed on the right side.</li>
            <li>You can remove any file by clicking the delete button (🗑️) next to the file name.</li>
        </ol>
    </div>

    <div class="upload-container">
        <div class="upload-box" id="uploadBox">
            <div class="icon">⬆️</div>
            <p>Drag and Drop files to upload or</p>
            <button id="browseBtn">Browse</button>
            <p>Supported files: PDF, DOC, DOCX, JPG</p>
            <input type="file" id="fileInput" multiple>
        </div>
        <div class="uploaded-files">
            <ul id="fileList">
                <% if @uploaded_files.present? %>
                    <% @uploaded_files.each do |file| %>
                        <li>
                            <img src="file-icon.png" alt="<%= file.file_type %> Icon">
                            <div class="file-info">
                                <div class="file-name">
                                    <%= file.name %>
                                </div>
                                <div class="file-size"><%= number_to_human_size(file.file_size) %></div>
                                <div class="progress-bar"><div style="width: <%= file.progress %>%"></div></div>
                                <div class="delete-btn" data-id="<%= file.id %>">🗑️</div>
                            </div>
                        </li>
                    <% end %>
                <% else %>
                    <p id="noFilesMessage">No files uploaded yet.</p>
                <% end %>
            </ul>
        </div>
    </div>

    <script>
        const uploadBox = document.getElementById('uploadBox');
        const fileInput = document.getElementById('fileInput');
        const browseBtn = document.getElementById('browseBtn');
        const fileList = document.getElementById('fileList');
        const noFilesMessage = document.getElementById('noFilesMessage');

        const supportedFormats = ['application/pdf', 'application/msword', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'image/jpeg'];
        const maxFileSize = 5 * 1024 * 1024; // 5MB in bytes

        browseBtn.addEventListener('click', () => {
            fileInput.click();
        });

        fileInput.addEventListener('change', (event) => {
            handleFiles(event.target.files);
        });

        uploadBox.addEventListener('dragover', (event) => {
            event.preventDefault();
            uploadBox.style.borderColor = '#42a5f5';
        });

        uploadBox.addEventListener('dragleave', () => {
            uploadBox.style.borderColor = '#d9e3f0';
        });

        uploadBox.addEventListener('drop', (event) => {
            event.preventDefault();
            uploadBox.style.borderColor = '#d9e3f0';
            const files = event.dataTransfer.files;
            handleFiles(files);
        });

        function handleFiles(files) {
            for (const file of files) {
                if (supportedFormats.includes(file.type)) {
                    if (file.size <= maxFileSize) {
                        const formData = new FormData();
                        formData.append('uploaded_file[file_path]', file);
                        formData.append('uploaded_file[name]', file.name);
                        formData.append('uploaded_file[file_type]', file.type);
                        formData.append('uploaded_file[file_size]', file.size);

                        fetch('<%= user_particular_uploaded_files_path(@user_particular) %>', {
                            method: 'POST',
                            headers: {
                                'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
                            },
                            body: formData
                        })
                        .then(response => response.json())
                        .then(file => {
                            const fileUrl = file.file_path_url;
                            const listItem = document.createElement('li');
                            listItem.innerHTML = `
                                <img src="file-icon.png" alt="${file.file_type} Icon">
                                <div class="file-info">
                                    <div class="file-name">
                                        ${file.name}
                                    </div>
                                    <div class="file-size">${formatFileSize(file.file_size)}</div>
                                    <div class="progress-bar"><div style="width: 0%"></div></div>
                                    <div class="delete-btn" data-id="${file.id}">🗑️</div>
                                </div>
                            `;
                            if (noFilesMessage) {
                                noFilesMessage.remove();
                            }
                            fileList.appendChild(listItem);
                            updateProgress(listItem, 0); // Initialize progress
                        })
                        .catch(error => console.error('Error:', error));
                    } else {
                        alert('File size exceeds 5MB limit');
                    }
                } else {
                    alert('Unsupported file format');
                }
            }
        }

        function formatFileSize(bytes) {
            const units = ['bytes', 'KB', 'MB', 'GB', 'TB'];
            let unitIndex = 0;
            let fileSize = bytes;
            while (fileSize >= 1024 && unitIndex < units.length - 1) {
                fileSize /= 1024;
                unitIndex++;
            }
            return fileSize.toFixed(2) + ' ' + units[unitIndex];
        }

        function updateProgress(listItem, progress) {
            const progressBar = listItem.querySelector('.progress-bar div');
            progressBar.style.width = progress + '%';
        }

        function removeFile(element) {
            const listItem = element.parentElement.parentElement;
            const fileId = element.getAttribute('data-id');

            fetch(`/user_particulars/<%= @user_particular.id %>/uploaded_files/${fileId}`, {
                method: 'DELETE',
                headers: {
                    'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').getAttribute('content')
                }
            })
            .then(response => {
                if (response.ok) {
                    listItem.remove();
                    if (!fileList.querySelector('li')) {
                        fileList.innerHTML = '<p id="noFilesMessage">No files uploaded yet.</p>';
                    }
                } else {
                    alert('Failed to delete file');
                }
            })
            .catch(error => console.error('Error:', error));
        }
    </script>
</body>
</html>
